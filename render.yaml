services:
  - type: web
    name: medigo-hospital-finder
    env: node
    plan: free
    buildCommand: npm install && npm rebuild sqlite3
    startCommand: node backend/server.js
    envVars:
      - key: PORT
        value: 10000
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        generateValue: true
